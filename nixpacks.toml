# Nixpacks Configuration for Wallet Farm Backend
# IMPORTANT: In Railway dashboard, set "Root Directory" to: wallet-farm
# This file is a fallback configuration

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci --omit=dev"]

[start]
cmd = "npm run server"
