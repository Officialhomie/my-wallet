# Railway Configuration for TVRCH Backend
# Deploy configuration - build is handled by nixpacks.toml

[deploy]
startCommand = "cd core && npm run server"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
